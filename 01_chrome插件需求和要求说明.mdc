# 01 Chrome 插件需求和要求说明

## 目标

实现一款 Chrome 插件：当用户选中网页中的段落或关键词时，提供以下功能，并以悬浮面板形式直接展现结果。

## 功能列表

1. 解释：解释选中的段落或关键词（Kimi API 实现即可）
2. 翻译：翻译选中的段落或关键词（支持中文和英文翻译，Kimi API 实现即可）
3. 朗读：朗读选中的段落或关键词（调用浏览器内置朗读，支持中文和英文朗读）
4. 润色和替换：对选中的文本文案进行润色（Kimi API 实现即可）

## 交互与展示要求

1. 当用户选中段落或关键词时，插件在选中区域上方悬浮出现。
2. 悬浮面板显示顺序固定为：解释 -> 翻译 -> 朗读 -> 润色。
3. 解释/翻译/润色的结果直接在悬浮面板下方显示。
4. 翻译需要语言选择：
   - 翻译目标：中文或英文二选一。
5. 润色结果支持修改：
   - 面板内可编辑润色结果文本。
   - 提供“一键替换”按钮：将编辑后的文本替换回页面中用户原先选中的内容（可编辑情况下可直接替换）。

## 技术要求

1. 解释、翻译、润色：使用 Moonshot/Kimi API 实现。
   - 参考文档：https://platform.moonshot.cn/docs
2. 朗读：调用 Chrome 浏览器内置语音功能即可（SpeechSynthesis），支持中文与英文朗读。

## 验收要点

1. 选中文本后悬浮面板自动出现且显示顺序正确。
2. 点击“解释/翻译/润色”后，结果在悬浮面板下方正确展示。
3. “翻译”可选择目标语言（中文/英文）。
4. “朗读”能够朗读选中原文，中文/英文语音正确（至少可用）。
5. “润色”结果可编辑，并且“一键替换”能替换回原页面选区文本。
6. 提供完整 README：说明如何在 Chrome 中部署安装插件。




